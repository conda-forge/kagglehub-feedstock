schema_version: 1

context:
  name: kagglehub
  version: "0.4.3"

package:
  name: ${{ name|lower }}
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/${{ name[0] }}/${{ name }}/kagglehub-${{ version }}.tar.gz
  sha256: 7969ae7574f5524dfa0f4fdb362229e1187cd5ea0dc0ef6e7ea5ca23478877ee

build:
  number: 0
  noarch: python
  script: ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation

requirements:
  host:
    - python ${{ python_min }}.*
    - hatchling
    - pip
  run:
    - python >=${{ python_min }}
    - requests
    - tqdm
    - packaging
    - pyyaml
    - kagglesdk >=0.1.14,<1.0

tests:
  - python:
      imports:
        - kagglehub
      pip_check: true
      python_version: ${{ python_min }}.*

about:
  summary: Access Kaggle resources anywhere
  license: Apache-2.0
  license_file: LICENSE
  homepage: https://github.com/Kaggle/kagglehub

extra:
  recipe-maintainers:
    - ngam
    - synapticarbors
